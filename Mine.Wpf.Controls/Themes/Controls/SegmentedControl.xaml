<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Mine.Wpf.Controls.Controls"
                    xmlns:conv="clr-namespace:Mine.Wpf.Controls.Converters">
    <conv:NullToVisibilityConverter x:Key="Local.NullToVisConv"/>
    <!-- 首/末/独项圆角（与外框 CornerRadius=20 匹配，MinHeight=40 → 半径20） -->
    <CornerRadius x:Key="Local.CornerRadius.Full">20</CornerRadius>
    <CornerRadius x:Key="Local.CornerRadius.Left">20,0,0,20</CornerRadius>
    <CornerRadius x:Key="Local.CornerRadius.Right">0,20,20,0</CornerRadius>
    <CornerRadius x:Key="Local.CornerRadius.None">0</CornerRadius>

    <!-- ═══ SegmentedItem ═══════════════════════════════════════════════ -->
    <Style TargetType="{x:Type controls:SegmentedItem}">
        <Setter Property="MinHeight"                  Value="40"/>
        <Setter Property="MinWidth"                   Value="48"/>
        <Setter Property="Padding"                    Value="16,0"/>
        <Setter Property="Cursor"                     Value="Hand"/>
        <Setter Property="FocusVisualStyle"           Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment"   Value="Center"/>
        <Setter Property="Background"                 Value="Transparent"/>
        <Setter Property="Foreground"                 Value="{DynamicResource Mine.Brush.OnSurface}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:SegmentedItem}">
                    <!--
                        LayoutRoot 按 Position 设置对应方向圆角，使选中背景自然贴合外框，
                        无需 ClipToBounds，外框边线不受影响。
                    -->
                    <Border x:Name="LayoutRoot"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource Local.CornerRadius.None}"
                            MinHeight="{TemplateBinding MinHeight}"
                            MinWidth="{TemplateBinding MinWidth}">
                        <Border x:Name="Separator"
                                BorderBrush="{DynamicResource Mine.Brush.Outline}"
                                BorderThickness="0,0,1,0">
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}">
                                <TextBlock x:Name="CheckIcon"
                                           Text="&#xE876;"
                                           FontFamily="{DynamicResource Mine.Font.MaterialSymbols}"
                                           FontSize="18"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"
                                           Margin="0,0,8,0"
                                           Visibility="Collapsed"/>
                                <TextBlock x:Name="IconText"
                                           Text="{TemplateBinding Icon}"
                                           FontFamily="{DynamicResource Mine.Font.MaterialSymbols}"
                                           FontSize="18"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"
                                           Margin="0,0,8,0"
                                           Visibility="Collapsed"/>
                                <ContentPresenter x:Name="Label"
                                                  VerticalAlignment="Center"
                                                  TextBlock.FontFamily="{DynamicResource Mine.FontFamily.Default}"
                                                  TextBlock.FontSize="{DynamicResource Mine.FontSize.LabelLarge}"
                                                  TextBlock.FontWeight="Medium"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}"/>
                            </StackPanel>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Icon, RelativeSource={RelativeSource Self},
                                               Converter={StaticResource Local.NullToVisConv}}"
                                     Value="Visible">
                            <Setter TargetName="IconText" Property="Visibility" Value="Visible"/>
                        </DataTrigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="LayoutRoot" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Mine.Color.OnSurface}" Opacity="0.08"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="LayoutRoot" Property="Background"
                                    Value="{DynamicResource Mine.Brush.SecondaryContainer}"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Mine.Brush.OnSecondaryContainer}"/>
                            <Setter TargetName="CheckIcon" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <!-- 首项：左侧圆角，保留右侧分隔线 -->
                        <Trigger Property="Position" Value="First">
                            <Setter TargetName="LayoutRoot" Property="CornerRadius"
                                    Value="{StaticResource Local.CornerRadius.Left}"/>
                        </Trigger>

                        <!-- 末项：右侧圆角，去掉分隔线 -->
                        <Trigger Property="Position" Value="Last">
                            <Setter TargetName="LayoutRoot" Property="CornerRadius"
                                    Value="{StaticResource Local.CornerRadius.Right}"/>
                            <Setter TargetName="Separator" Property="BorderThickness" Value="0"/>
                        </Trigger>

                        <!-- 独项：两侧圆角，去掉分隔线 -->
                        <Trigger Property="Position" Value="Only">
                            <Setter TargetName="LayoutRoot" Property="CornerRadius"
                                    Value="{StaticResource Local.CornerRadius.Full}"/>
                            <Setter TargetName="Separator" Property="BorderThickness" Value="0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══ SegmentedControl ════════════════════════════════════════════ -->
    <Style TargetType="{x:Type controls:SegmentedControl}">
        <Setter Property="Focusable"        Value="False"/>
        <Setter Property="BorderThickness"  Value="1"/>
        <Setter Property="BorderBrush"      Value="{DynamicResource Mine.Brush.Outline}"/>
        <Setter Property="Background"       Value="Transparent"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility"   Value="Disabled"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:SegmentedControl}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource Local.CornerRadius.Full}">
                        <ItemsPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.38"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

